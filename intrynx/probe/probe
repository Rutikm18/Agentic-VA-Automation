#!/usr/bin/env bash
# Intrynx Probe launcher.  Usage:  ./probe [run|check|setup|hostid|version]
# The agent loads probe.env itself (values with spaces are handled safely).
set -euo pipefail
cd "$(dirname "$0")"
exec python3 agent.py "$@"
