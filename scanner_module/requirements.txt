# Core scanners (host_discovery, port_scanner, service_banner, tls_scanner,
# udp_scanner, smb_scanner, snmp_scanner, web_scanner, mcp_ai_scanner) use the
# Python standard library ONLY — no install required.

# Optional, per-scanner:

# ssh_collector.py — credentialed Linux inventory collection
paramiko>=3.4.0

# nmap_wrapper.py — requires the `nmap` binary installed on the system
# (apt install nmap / brew install nmap). No Python package needed; it shells
# out and parses XML with the stdlib.

# Optional convenience for richer SNMP if you later replace the minimal probe:
# pysnmp>=5.0.0

# mass_scan.py — for true large-scale speed, install the masscan binary
# (apt install masscan). Falls back to a pure-Python connect sweep if absent.
# masscan is AGPL-3.0 — review licensing before bundling in a product.

# db_scanner.py — pure stdlib, no install needed.

# windows_collector.py — credentialed Windows inventory
#   WinRM path:  pip install pywinrm
#   SMB fallback path:  pip install impacket
# Either transport alone is usable; install both for full graceful degradation.
pywinrm>=0.4.3
